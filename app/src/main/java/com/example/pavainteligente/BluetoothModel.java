package com.example.pavainteligente;

public class BluetoothModel //implements BluetoothContract.Model
{
/*
    private ArrayList<BluetoothDevice> dispositivos;
    //se crea un array de String con los permisos a solicitar en tiempo de ejecucion
    //Esto se debe realizar a partir de Android 6.0, ya que con verdiones anteriores
    //con solo solicitarlos en el Manifest es suficiente
    private final String[] permissions;

    public BluetoothModel() {
        dispositivos = new ArrayList<>();
        permissions = new String[]{
                Manifest.permission.BLUETOOTH,
                Manifest.permission.BLUETOOTH_ADMIN,
                Manifest.permission.ACCESS_COARSE_LOCATION,
                Manifest.permission.BLUETOOTH_CONNECT,
                Manifest.permission.BLUETOOTH_SCAN,
                Manifest.permission.WRITE_EXTERNAL_STORAGE,
                Manifest.permission.READ_PHONE_STATE,
                Manifest.permission.READ_EXTERNAL_STORAGE,
                Manifest.permission.BLUETOOTH_ADVERTISE,
                Manifest.permission.ACCESS_FINE_LOCATION,
                //Manifest.permission.ACCESS_BACKGROUND_LOCATION
        };
    }

    @Override
    public void addDispositivo(BluetoothDevice dispositivoNuevo) {
        dispositivos.add(dispositivoNuevo);
    }

    @Override
    public ArrayList<BluetoothDevice> getListaDispositivos() {
        return dispositivos;
    }

    @Override
    public void setListaDispositivos(ArrayList<BluetoothDevice> dispositivosEncontrados) {
        dispositivos = dispositivosEncontrados;
    }

    @Override
    public String[] getPermissions() {
        return permissions;
    }

 */
}
